 export async function randomWait() {
    // let waitTime = Math.floor(Math.random() * 20) + 1;
    // await new Promise((resolve) => setTimeout(resolve, waitTime * 1000));
    console.log('waited for ' + waitTime + ' seconds')
}

